{
  "name": "pokefan",
  "version": "1.0.0",
  "description": "Pokémon fan project with authentication and favorites",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
